default:
  cache:
    key: terraform-cache
    paths:
      - .terraform/

stages:
  - init
  - validate
  - plan

variables:
  TF_IN_AUTOMATION: "true"
  TF_LOG: "DEBUG"

init:
  stage: init
  image:
    name: hashicorp/terraform:latest
    entrypoint: [""]
  tags:
    - docker
  script:
    - terraform --version
    - terraform init
    - ls -la

validate:
  stage: validate
  image:
    name: hashicorp/terraform:latest
    entrypoint: [""]
  tags:
    - docker
  dependencies:
    - init
  script:
    - terraform init -backend=false
    - terraform validate

plan:
  stage: plan
  image:
    name: hashicorp/terraform:latest
    entrypoint: [""]
  tags:
    - docker
  dependencies:
    - init
  script:
    - chmod +x scripts/download-tfvars.sh
    - ./scripts/download-tfvars.sh
    - terraform plan -var-file=terraform.tfvars -out=tfplan
  artifacts:
    paths:
      - tfplan
    expire_in: 1 hour